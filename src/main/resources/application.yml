spring:

  datasource:
    url: jdbc:mysql://localhost:3306/TheBarber?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 2402
    driver-class-name: com.mysql.cj.jdbc.Driver

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  h2:
    console:
      enabled: false

notification:
  exchange: notification.exchange
  routing-key: notification.whatsapp

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

logging:
  level:
    com.theBarber.TheBarber.WSServices.WhatsAppClient: DEBUG

jwt:
  secret: "3z!8Ue$L@V7jR9p^YgBxTzQ*FgPqHsKm"

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    prometheus:
      enabled: true
  metrics:
    enable:
      all: true
kind: Application