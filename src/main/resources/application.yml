spring:
  datasource:
    url: jdbc:mysql://localhost:3306/TheBarber?useSSL=false&serverTimezone=UTC
    username: root
    password: 2402
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  h2:
    console:
      enabled: false

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

logging:
  level:
    com.theBarber.TheBarber.WSServices.WhatsAppClient: DEBUG

jwt:
  secret: "3z!8Ue$L@V7jR9p^YgBxTzQ*FgPqHsKm"

